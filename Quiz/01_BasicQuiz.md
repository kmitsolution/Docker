## **Docker MCQ (Beginner Level)**

### **1. What is Docker primarily used for?**

A. Creating virtual machines
B. Managing physical servers
C. Containerizing applications
D. Writing source code

---

### **2. A Docker container is best described as:**

A. A full virtual machine
B. A lightweight, standalone executable package
C. A hardware emulator
D. A programming framework

---

### **3. Which command is used to build a Docker image from a Dockerfile?**

A. `docker create`
B. `docker build`
C. `docker run`
D. `docker image`

---

### **4. What is the default name of the file used to build a Docker image?**

A. DockerScript
B. Containerfile
C. Dockerfile
D. Buildfile

---

### **5. Which command lists all running containers?**

A. `docker ps`
B. `docker show`
C. `docker list`
D. `docker containers`

---

### **6. Which Docker command is used to download an image from Docker Hub?**

A. `docker load`
B. `docker copy`
C. `docker pull`
D. `docker clone`

---

### **7. Docker Hub is a:**

A. Local file system
B. Cloud registry for Docker images
C. Docker monitoring tool
D. Payment service

---

### **8. What does the `docker run` command do?**

A. Uploads an image
B. Deletes containers
C. Creates and starts a container
D. Creates a Dockerfile

---

### **9. Which command stops a running Docker container?**

A. `docker kill`
B. `docker exit`
C. `docker stop`
D. `docker quit`

---

### **10. What is the purpose of `docker-compose.yml`?**

A. To monitor logs
B. To define and run multi-container apps
C. To update Docker
D. To store credentials

---

### **11. The command `docker images` is used to:**

A. Show all containers
B. Show available Docker images
C. Remove images
D. Build images

---

### **12. What is an image in Docker?**

A. A running process
B. A template used to create containers
C. A log file
D. A virtual machine

---

### **13. Which layer changes when a container is running?**

A. Read-only image layer
B. Temporary RAM layer
C. Writable container layer
D. Kernel layer

---

### **14. Which command gives you logs of a container?**

A. `docker output`
B. `docker logs`
C. `docker show`
D. `docker trace`

---

### **15. Docker is based on which Linux kernel feature?**

A. Hypervisor
B. Cgroups and namespaces
C. BIOS virtualization
D. Docker kernel module

---

## ✅ **Answers**

1 — C
2 — B
3 — B
4 — C
5 — A
6 — C
7 — B
8 — C
9 — C
10 — B
11 — B
12 — B
13 — C
14 — B
15 — B

